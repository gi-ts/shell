{
  "name": "@gi.ts/packages-shell8",
  "private": true,
  "devDependencies": {
    "@gi.ts/cli": "^1.5.7",
    "@gi.ts/lib": "^1.5.9",
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/@gi-types/**"
  ],
  "scripts": {
    "build": "tsc",
    "pub": "lerna publish",
    "pub:package": "lerna publish from-package",
    "generate": "XDG_DATA_DIRS=$INSTALL_PREFIX/lib/mutter-8/:$INSTALL_PREFIX/share/gnome-shell/:$XDG_DATA_DIRS gi-ts generate docs.json"
  }
}
